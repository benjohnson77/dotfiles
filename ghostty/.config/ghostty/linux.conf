# Linux-only Ghostty settings. Included from `config` via `config-file = ?linux.conf`,
# so macOS simply skips it.

# Fix general slowness on Hyprland (https://github.com/ghostty-org/ghostty/discussions/3224)
async-backend = epoll
